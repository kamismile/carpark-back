server:
  port: ${PORT_TO_EXPOSE:8080}

eureka:
  instance.preferIpAddress: true
  client.serviceUrl.defaultZone: http://service-registry.netflix:8080/

zuul:
  sensitiveHeaders:
  routes:
    auth:
      serviceId: auth
      sensitiveHeaders:

---
spring:
  profiles: dev

server:
  port: 9099

eureka:
  instance.preferIpAddress: true
  client.serviceUrl.defaultZone: http://localhost:8761/eureka/